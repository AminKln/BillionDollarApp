# Secrets — never commit real tokens/keys
.env
*.pem
*.key

# Python
__pycache__/
*.pyc
.venv/
venv/

# SAM / CDK build output
.aws-sam/
cdk.out/

# OS / editor
.DS_Store
Thumbs.db

# local process state (pids, logs from scripts/seed_metrics.py)
.run/
